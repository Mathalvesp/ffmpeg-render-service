<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" content="#0b0c0e" />
  <title>Domus Brickell Center | Imóvel em Miami</title>
  <meta name="description" content="Conheça o Domus Brickell Center em Brickell, Miami: unidades finalizadas e mobiliadas, hospitalidade profissional e estrutura turnkey." />
  <meta property="og:title" content="Domus Brickell Center | Imóvel em Miami" />
  <meta property="og:description" content="Uma nova forma de viver e investir em Brickell." />
  <meta property="og:type" content="website" />
  <meta name="twitter:card" content="summary_large_image" />
  <style>
    :root{
      --ink:#111214;
      --night:#090b0e;
      --paper:#f3f0e8;
      --paper-2:#e9e5da;
      --muted:#ada99f;
      --line:rgba(20,20,20,.14);
      --line-light:rgba(255,255,255,.16);
      --accent:#c9a878;
      --max:1380px;
      --pad:clamp(20px,4vw,64px);
      --ease:cubic-bezier(.2,.65,.25,1);
    }

    *{box-sizing:border-box}
    html{scroll-behavior:smooth;background:var(--night)}
    body{margin:0;background:var(--paper);color:var(--ink);font-family:Inter,ui-sans-serif,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;line-height:1.45;overflow-x:hidden}
    body.menu-open{overflow:hidden}
    a{color:inherit;text-decoration:none}
    img{display:block;width:100%;height:auto}
    button,input{font:inherit}
    ::selection{background:var(--ink);color:var(--paper)}

    .serif{font-family:"Iowan Old Style","Baskerville","Times New Roman",serif;font-weight:400;letter-spacing:-.035em}
    .eyebrow{font-size:11px;letter-spacing:.22em;text-transform:uppercase;font-weight:650}
    .container{width:min(var(--max),100%);margin-inline:auto;padding-inline:var(--pad)}
    .section{position:relative;padding:clamp(88px,11vw,180px) 0}
    .section-dark{background:var(--night);color:#f5f2ea}
    .section-soft{background:var(--paper-2)}
    .hairline{height:1px;background:var(--line)}
    .btn{display:inline-flex;align-items:center;gap:14px;justify-content:center;min-height:52px;padding:0 22px;border:1px solid currentColor;background:transparent;color:inherit;text-transform:uppercase;font-size:11px;font-weight:700;letter-spacing:.14em;cursor:pointer;transition:background .35s var(--ease),color .35s var(--ease),transform .35s var(--ease)}
    .btn:hover{background:var(--ink);color:var(--paper);transform:translateY(-2px)}
    .section-dark .btn:hover,.hero .btn:hover{background:#f4f1e9;color:var(--night)}
    .btn-arrow::after{content:"↗";font-size:15px;line-height:1;transition:transform .35s var(--ease)}
    .btn-arrow:hover::after{transform:translate(3px,-3px)}

    /* Header */
    .header{position:fixed;inset:0 0 auto 0;z-index:100;display:flex;align-items:center;justify-content:space-between;height:86px;padding:0 var(--pad);color:#fff;transition:height .35s var(--ease),background .35s var(--ease),backdrop-filter .35s var(--ease),color .35s var(--ease),border .35s var(--ease)}
    .header.scrolled{height:68px;background:rgba(9,11,14,.82);backdrop-filter:blur(14px);border-bottom:1px solid var(--line-light)}
    .brand{display:flex;align-items:center;gap:14px}
    .brand-word{font-size:28px;letter-spacing:.38em;text-transform:lowercase;font-weight:350;line-height:1}
    .brand-sub{font-size:8px;letter-spacing:.34em;text-transform:uppercase;opacity:.78;margin-top:6px}
    .nav{display:flex;align-items:center;gap:30px}
    .nav a{font-size:10px;letter-spacing:.15em;text-transform:uppercase;opacity:.78;transition:opacity .2s}
    .nav a:hover{opacity:1}
    .nav-cta{border-bottom:1px solid rgba(255,255,255,.55);padding-bottom:5px}
    .menu-btn{display:none;background:none;border:0;color:#fff;width:40px;height:40px;position:relative;cursor:pointer}
    .menu-btn::before,.menu-btn::after{content:"";position:absolute;left:8px;right:8px;height:1px;background:currentColor;transition:.3s var(--ease)}
    .menu-btn::before{top:15px}.menu-btn::after{top:24px}
    .menu-open .menu-btn::before{transform:translateY(4.5px) rotate(45deg)}
    .menu-open .menu-btn::after{transform:translateY(-4.5px) rotate(-45deg)}

    /* Hero */
    .hero{height:100svh;min-height:680px;position:relative;background:#05070a;color:#fff;overflow:hidden}
    .hero-media{position:absolute;inset:-5%;will-change:transform;transform:scale(1.06)}
    .hero-media img{width:100%;height:100%;object-fit:cover;object-position:50% 50%;filter:saturate(.88) contrast(1.03)}
    .hero::before{content:"";position:absolute;inset:0;z-index:2;background:linear-gradient(180deg,rgba(3,5,7,.5) 0%,rgba(3,5,7,.12) 42%,rgba(3,5,7,.68) 100%)}
    .hero::after{content:"";position:absolute;inset:0;z-index:2;background:radial-gradient(circle at 65% 36%,transparent 0 25%,rgba(0,0,0,.12) 70%)}
    .hero-inner{position:relative;z-index:3;height:100%;display:flex;flex-direction:column;justify-content:flex-end;padding:0 var(--pad) clamp(42px,7vh,80px);width:min(1500px,100%);margin-inline:auto}
    .hero-kicker{display:flex;align-items:center;gap:14px;margin-bottom:22px;opacity:.82}
    .hero-kicker::before{content:"";width:48px;height:1px;background:rgba(255,255,255,.7)}
    .hero h1{font-size:clamp(54px,8.1vw,126px);line-height:.87;margin:0;max-width:1160px;text-wrap:balance}
    .hero h1 span{display:block;font-size:.48em;line-height:1.06;margin-top:14px;letter-spacing:-.025em;max-width:900px}
    .hero-bottom{display:flex;justify-content:space-between;align-items:flex-end;gap:32px;margin-top:36px}
    .hero-copy{max-width:590px;font-size:clamp(14px,1.3vw,18px);color:rgba(255,255,255,.82);margin:0}
    .scroll-cue{display:flex;align-items:center;gap:12px;font-size:10px;letter-spacing:.18em;text-transform:uppercase;white-space:nowrap;opacity:.72}
    .scroll-cue i{width:36px;height:36px;border:1px solid rgba(255,255,255,.4);border-radius:50%;position:relative}
    .scroll-cue i::after{content:"↓";position:absolute;inset:0;display:grid;place-items:center;font-style:normal;animation:bob 1.9s ease-in-out infinite}
    @keyframes bob{50%{transform:translateY(4px)}}

    /* Intro */
    .intro-grid{display:grid;grid-template-columns:.72fr 1.28fr;gap:clamp(40px,8vw,130px);align-items:start}
    .intro-title{position:sticky;top:120px}
    .intro-title h2{font-size:clamp(45px,6vw,94px);line-height:.95;margin:16px 0 0;text-wrap:balance}
    .intro-copy{font-size:clamp(20px,2vw,30px);line-height:1.35;letter-spacing:-.02em;margin:0 0 60px;max-width:790px}
    .intro-detail{display:grid;grid-template-columns:1fr 1fr;gap:28px;border-top:1px solid var(--line);padding-top:24px;color:#514f49}
    .intro-detail p{margin:0;font-size:14px;line-height:1.65}
    .editorial-image{margin-top:80px;height:min(76vw,900px);max-height:900px;overflow:hidden;position:relative;background:#d9d3c7}
    .editorial-image img{height:120%;object-fit:cover;will-change:transform;transform:translateY(-8%)}
    .image-caption{position:absolute;left:18px;